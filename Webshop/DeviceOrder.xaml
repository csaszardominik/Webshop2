<Window x:Class="Webshop.DeviceOrder"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Webshop"
        mc:Ignorable="d"
        Width="300"
        Height="450"
        FontSize="20"
        WindowStartupLocation="CenterScreen"
        Title="DeviceOrder"
        Background="CornflowerBlue">
    <StackPanel Background="LightSlateGray" Margin="20">
        <TextBlock
        HorizontalAlignment="Center"
        VerticalAlignment="Top"
        FontSize="25"
        FontWeight="Bold"
        Text="Device Order" />
        <Grid VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock
            Grid.Row="0"
            HorizontalAlignment="Left"
            Text="Users:" />
            <ComboBox
            x:Name="UserCmbx"
            Grid.Row="1"
            Width="250"
            HorizontalAlignment="Left">
            </ComboBox>
            <TextBlock
            Grid.Row="2"
            HorizontalAlignment="Left"
            Text="Devices:" />
            <ComboBox
            x:Name="DeviceCmbx"
            Grid.Row="3"
            Width="250"
            HorizontalAlignment="Left">
            </ComboBox>
        </Grid>
        <Button
        x:Name="OrderBtn"
        Width="200"
        Height="35"
        Margin="0,50,0,0"
        Background="DarkGray"
        Click="OrderBtn_Click"
        Content="Order"
        Foreground="White" />



    </StackPanel>
</Window>
